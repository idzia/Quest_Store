<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Admin Homepage</title>
        <link rel="stylesheet" href="../../static/css/admin_styles.css">
        <link rel="stylesheet" href="../../static/css/styles.css">
        <link rel="stylesheet" href="../../static/css/user_styles.css">
        
        <link href="https://fonts.googleapis.com/css?family=Nunito" rel="stylesheet">
    </head>

    <body>
    <div class="content">
        <header>
            <div id="user">
                <a href="mentor"><img src="../../static/img/user.jpg" alt="avatar" ></a>
            </div>

            <div class="info">
                <div class="info-center">
                    <h1>Hell {{userName}}</h1>
                </div>
                <div class="info-top">
                    <a href="login"><img src="../../static/img/out.png" alt="logout" title="Log out"></a>
                </div>
            </div>
        </header>

        <div class="bar-content-block">
            <div class="bar">
                <ul class="menu">
                    <li class="menu-item"><a href="/admin">Mentor</a></li>
                    <li class="menu-item"><a href="/class" class="selected">Class</a></li>
                    <li class="menu-item"><a href="/level">Level</a></li>
                </ul>
            </div>

            <div class="content-block">
                <h1>Classes</h1>
                <table class="show-mentors">
                    <thead>
                        <tr>
                            <th>Class</th>
                            <th>Mentors</th>
                        </tr>
                    </thead>

                    <tbody>
                        {% for key, value in classMentorMap %}
                            <tr>
                                <td rowspan={{classMentorMap.size()}}>{{key}}</td>

                                {% for name in value%}

                                    <td>{{name}}</td></tr><tr>

                                {% endfor %}

                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
                <form>
                    <input class="add-class-button" onclick="window.location.href='addNewClass.html'" type="button" value="Add">
                </form>
            </div>
        </div>
    </div>

    <footer>
        <a href='https://www.freepik.com/free-vector/assorted-user-avatars-collection_851876.htm'>Designed by Freepik</a>
        <p>MADD crew Production</p>
    </footer>
    </body>
</html>
